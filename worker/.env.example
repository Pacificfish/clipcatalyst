# Copy this file to .env (or set these in your deployment environment)
# and fill in values as needed.

# Optional shared secret for authenticating requests to the worker
SHARED_SECRET=

# Vercel Blob (preferred storage when set)
# Generate a Read-Write token in Vercel (Storage > Blob) and set it here.
# When present, the worker uploads outputs to Vercel Blob and returns public URLs.
BLOB_READ_WRITE_TOKEN=

# AWS S3 (fallback storage if Blob token is not set)
# Provide these only if you want to use S3. If both Blob and S3 are configured,
# Blob will be used.
S3_BUCKET=
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# AssemblyAI (only required for /transcribe_assembly and /suggest_highlights)
ASSEMBLYAI_API_KEY=

# Optional: override ffmpeg/ffprobe paths (auto-detected otherwise)
FFMPEG_PATH=
FFPROBE_PATH=

# Optional caption tuning
CAPTION_TIMING_SCALE=1
CAPTION_SHIFT_MS=0

# Server port (default 8080)
PORT=8080

